<h1>Listado de productos.</h1>

{{#if productos}}
    <ul>
        {{#each productos}}
            <li>{{this}}</li>
        {{/each}}
    </ul>
{{else}}
    <h2>No hay productos para mostrar.</h2>
{{/if}}